import * as React from "react"
import { useStaticQuery, graphql } from "gatsby"

const topUI = () => {
  return (
    <div className="topUI">
      <h5>성훈 블로그</h5>
    </div>
  )
}

export default topUI
